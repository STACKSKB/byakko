[package]
name = "byakko_ast_audit"
version = "0.1.0"
edition = "2021"

[dependencies]
syn = { version = "=2.0.119", features = ["full", "visit"] }

# Standalone research tool, outside the product workspace.
[workspace]
