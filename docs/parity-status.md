# Configurator acceptance ledger

This ledger separates implemented configuration storage from end-to-end behavior. Source observations live in `feature-inventory.md`; attached-device evidence lives in `live-evidence.md`. Neither a shared OEM encoder nor an advertised catalog proves that every feature works on this board.

The table combines retained research evidence with the current Iced migration.
Iced has keymap, macro, built-in global lighting, per-key picture and scalar
settings editing/read/save slices, plus native archive capture/export/review/apply.
Physical acceptance remains narrower than implemented behavior. See
`iced-keymap-acceptance.md`, `iced-macro-acceptance.md`,
`iced-lighting-acceptance.md`, `iced-picture-acceptance.md` and
`iced-settings-acceptance.md` and `iced-archive-acceptance.md` for their exact
scope.

| Requirement | Current evidence | Remaining acceptance work |
| --- | --- | --- |
| Native product without JS, Electron or IoT helper | Rust/Iced desktop release builds; direct HID reads/writes work with OEM helper stopped. Target-filtered desktop and CLI dependency metadata passes the offline permissive-license gate. A deterministic offline inventory covers the 182-package union; the 11 crate archives lacking root license texts now have exact-commit upstream copies in `packaging/notices`. | Assemble the complete dependency notice bundle, resolve compound obligations, complete source/asset audit and distributable packaging |
| Resource use | A same-host, attached-board warmed-idle sample found Byakko Iced at 25.6 MiB mean working set and 10.6 MiB mean private memory, versus Sharkfin and its WebView2 process tree at 426.7 MiB and 225.5 MiB respectively; see `performance-baseline.md` for method and limits. A later Byakko-only sample remained stable. | Compare matched pages and active workloads, including startup and sustained streaming; measure the official app separately |
| Reusable frontend | Iced feature views render portable descriptors and capabilities without Nia87 protocol branches; memory backend exercises different layout/layers. The independent CLI reads keymaps, color, lighting, settings and macro slots through the same session/executor. It plans and applies keymap, one-field settings, global-lighting, macro-slot and per-key color snapshots against fresh raw revisions through that contract; memory-backend tests cover write/readback and stale-file rejection. Physical CLI keymap, macro-slot, global-lighting and one-field settings write/restore cycles now pass; per-key color writes remain pending. The core checks for `wasm32-unknown-unknown`. Shared HID inventory discovered eleven Wacom tablet collections without tablet protocol code; a synthetic tablet descriptor passes the portable ExpressKey/discrete-ring draft and apply contract. | Add QMK/VIA discovery and adapter after Nia87 acceptance; model continuous tablet controls only with a concrete backend contract; build a static web adapter within browser permission limits |
| Linux configuration | The desktop, CLI and narrow permission helper build on Debian 13; package tests and Clippy passed through `3ed3f3e`. The in-memory desktop demo started on X11. The exact `3151:4015` `hidraw2` descriptor is allowlisted by the helper, and the app accepts complete 64-byte payloads or complete 65-byte replies with a leading zero. On 2026-09-23, the node had the active-user ACL; a raw read-only `0x80` exchange returned 65 bytes including the leading zero. The normal-user CLI identified firmware `0x0100`, profile 0, and two keymap reads matched byte for byte. | Read-only reads for the other feature sections; Linux GUI, Wayland, and physical write/readback/restoration remain unverified (see `linux-handoff.md`) |
| Plug-and-play Nia87 TKL | Attached device auto-loads built-in 87-key geometry and fixed slots. The user observed the Iced release opening directly with verified readback and confirmed the TKL-shaped board and key selection. A physical unplug/replug was observed by read-only HID inventory, and a fresh keymap read matched the before capture byte for byte. Each connection's executor binds an immutable candidate identity; every transaction open, including recovery readback, checks that identity. Multiple matches and enumeration errors are explicit; headless reconnect tests retain staged drafts and reject stale results. Failed feature writes and conflicts require a manual read after reconnect; a multi-panel conflict test now preserves both specific warnings through disconnect. | Clean-machine and Linux GUI acceptance; stronger board/revision identification, reused-path identity limits and rendered cross-panel reconnect validation |
| Base and Fn keymaps | Both maps read twice; base, Fn-only and mixed same-slot application writes/restores pass with one-second setter spacing. The independent CLI also applied a single base A→B edit to slot9 with backup/readback, then restored A from a fresh revision; complete before/after archives compared `[]`. | Broader action coverage, physical output and persistence |
| Modifier, media and mouse actions | Pure codecs and catalog, including 19 selected official keyboard-catalog actions with documented wire facts. Three further system power/sleep/wake records now have named catalog entries and exact-byte descriptor tests; they have no physical output acceptance. Iced composes one- and two-modifier shortcuts from backend-advertised choices; a separate memory keyboard stages Ctrl+C and applies Ctrl+Shift+C with verified readback, and the Nia87 codec round-trips both. | Physical output and broader action behavior coverage, especially system controls that could affect host power |
| Macro storage | Exact 248-byte → short → empty replacement verified in slots1/24/49; 33 mixed key/mouse/movement events and delay boundaries stored exactly in slot49, then full configuration restoration verified; official short-macro capture now has a complete 64-byte replay regression. The official configurator advertises 50 slots (0–49). A read-only slot50 probe returned a stable but invalid macro buffer; no setter or binding was sent and a complete archive remained unchanged. The independent CLI twice applied a two-event macro to unbound slot49 with durable backup and full readback, then restored its original empty/count-zero raw bytes. The new product CLI native-backup restore path completed the second cycle; complete before/after archives compared `[]`. | Physical playback/timing and power-cycle persistence; repeat-zero and movement-delay semantics; do not infer usable slot50 storage from its getter alone |
| Macro editor | Native event table, focused keyboard/mouse-button recorder, repeats/modes, local labels, import/export and saved-slot binding; base/Fn binding modes read back and restore; recorder intervals attach to the preceding action; fixed-delay recording, reversible Clear draft and terminal-delay policy covered by pure/headless tests. Opening the page reads the selected unloaded/invalidated slot after keymap readiness in memory tests. The slot selector now grids capability-provided slots responsively with scrollbar clearance. Toggle/hold require explicitly staged count 1. Counted mode stages 1–65,535 while existing raw zero remains readable and lossless; save/bind with zero is blocked. | Rendered GUI interaction validation; physical playback modes and timing, including repeat-zero semantics |
| Global lighting | Native controls; selected cases for IDs0–19 pass write/read/restore; webcam confirms steady red/green. Iced reads unloaded/invalidated lighting on page entry in memory tests. The independent CLI now planned, backed up, applied and verified a steady-green color, then restored the original `(8,8,8)` lighting reply exactly. The camera showed green, and complete archives before and after compared `[]`. A backup permission failure stopped before the setter and left the raw reply unchanged. A prior read-only archive found effect 1 instead of the old effect-5 baseline, with all other sections unchanged. | Broader visual/option coverage and reactive effects; rendered Iced apply acceptance; identify earlier lighting drift cause |
| Per-key colors | Retained research GUI has native physical layout editor; earlier single-color write/restore passed, while selected official bulk reader/writer addresses index0 and has three separate global effect options (see `Research/picture-selector-audit.md`). Iced now has a capability-driven RGB editor, shared physical board and automatic read on page entry when stale. A live read-only CLI pass returned all 87 mapped colors; its guarded file plan reported one proposed change without a setter. RGB storage is separate from selecting the global picture effect. | Iced page interaction and visual validation; physical CLI and Iced per-key write/read/restore acceptance; effect-option semantics |
| Host-driven lighting | Captured actual0D music/0E screen commands; retained Windows screen/WASAPI streams passed hardware/camera/restoration checks. Iced screen-average and playback music use the same pure session lifecycle, bounded executor frames and selected-target Nia87 adapter; Stop and close require restoration, with headless failure/ordering tests. Music has capability-driven temporary brightness, option and fixed RGB/rainbow controls with pure report tests. A recognized stored host mode can now be explicitly replaced with an onboard effect through the guarded lighting contract; unknown modes remain opaque. Linux samplers cross-build. | Physical Iced screen/music streaming, parameter and stored-mode exit acceptance; close/focus-loss/disconnect acceptance, sustained streaming, Wayland capture, Linux runtime/audio routing, X11 disconnect handling |
| Other settings | Native debounce, auto-OS, four sleep timers and backlight switch pass reversible complete settings readback. Iced exposes these seven fields through capability data with one-field staged saves and automatic read on page entry in memory tests; its read-only 256-byte USB capture matches the earlier complete archive exactly. The independent CLI applied debounce 1→2→1 ms with durable backup and full readback; complete before/after archives compared `[]`. | Iced physical write/read/restore and rendered UI acceptance; physical sleep behavior and remaining option semantics; report-rate metadata exists but the selected official Nia87 descriptor does not enable its control (see `Research/report-rate-selected-path.md`) |
| Local configurations | Retained research app has full native archive capture, review and guarded apply; normal multi-section round trip passed, while first injected failure failed automatic recovery and an explicit later restore succeeded. Iced now captures, exports, reviews with forward/reverse preflight and offers guarded apply with typed recovery. The independent CLI completed another read-only full archive through the same session/executor; its embedded native bytes matched two earlier CLI captures exactly. A CLI review of the old baseline found only the known lighting change. No live Iced archive write has been attempted. | Resolve unexplained changes during failed recovery; repeat physical failure-injection acceptance for the typed path and validate OS GUI interaction |
| Robust device transactions | Expected-state checks, disk backups, readback/rollback and OS-held interprocess lock; optional blocking Windows USB string reads removed | USB error recovery, disconnect/reconnect and partial failure tests |
| 2.4 GHz | Deferred by USB-first scope | Receiver identification, transport capability and separate verification when available |

Iced screen-average and playback music share a distinct host stream lifecycle.
Physical Iced acceptance remains open. Host
lighting is separate from per-key RGB picture storage.

Firmware flashing, vendor account login and cloud sharing are outside the local stock-firmware configurator scope. OEM database metadata and UI arrangements are not product implementation sources. Further physical output and write-recovery tests remain explicitly pending while the user is AFK.
