# Configurator acceptance ledger

Current Linux update (2026-09-25, `f8583d7`): one read-only CLI pass succeeded
for keymap, lighting, settings, selector-bound colors (`[4,0]`), macro49 and a
complete archive, using the existing normal-user ACL. The archive hash matches
prior Linux captures. This closes the latest Linux selector-context read check;
it does not close physical writes, GUI, persistence or recovery. See
[Linux handoff](linux-handoff.md#current-read-only-acceptance-2026-09-25-source-f8583d7).
The earlier acceptance records below retain their dates and narrower scopes.

This ledger separates implemented configuration storage from end-to-end behavior. Source observations live in `feature-inventory.md`; attached-device evidence lives in `live-evidence.md`. Neither a shared OEM encoder nor an advertised catalog proves that every feature works on this board.

The table combines retained research evidence with the current Iced migration.
Iced has keymap, macro, built-in global lighting, per-key picture and scalar
settings editing/read/save slices, plus native archive capture/export.
The user-directed public pre-alpha archive page is capture/export-only; retained
review/restore APIs are developer research tools outside the public workflow.
Physical acceptance remains narrower than implemented behavior. See
`iced-keymap-acceptance.md`, `iced-macro-acceptance.md`,
`iced-lighting-acceptance.md`, `iced-picture-acceptance.md` and
`iced-settings-acceptance.md` and `iced-archive-acceptance.md` for their exact
scope.

| Requirement | Current evidence | Remaining acceptance work |
| --- | --- | --- |
| Native product without JS, Electron or IoT helper | Rust/Iced desktop release builds; direct HID reads/writes work with OEM helper stopped. Target-filtered desktop and CLI dependency metadata passes the offline permissive-license gate. A deterministic offline inventory covers the 182-package union; the 11 crate archives lacking root license texts now have exact-commit upstream copies in `packaging/notices`. | Assemble the complete dependency notice bundle, resolve compound obligations, complete source/asset audit and distributable packaging |
| Resource use | A same-host, attached-board warmed-idle sample found Byakko Iced at 25.6 MiB mean working set and 10.6 MiB mean private memory, versus Sharkfin and its WebView2 process tree at 426.7 MiB and 225.5 MiB respectively; see `performance-baseline.md` for method and limits. A later Byakko-only sample remained stable. | Compare matched pages and active workloads, including startup and sustained streaming; measure the official app separately |
| Reusable frontend | Iced feature views and the independent CLI use the same session/executor contract. CLI file planning reads once for its baseline; executor writes use the cached before-image and feature-specific completion semantics. CLI keymap, macro, global-lighting and one-field settings write/restore cycles have physical evidence; per-key CLI picture writes remain unaccepted. The core checks for `wasm32-unknown-unknown`; shared inventory and synthetic tablet contract evidence remain as recorded. | Add QMK/VIA discovery and adapter after Nia87 acceptance; model continuous tablet controls only with a concrete backend contract; build a static web adapter within browser permission limits |
| Linux configuration | The desktop, CLI and narrow permission helper built on Debian 13; the offline build, pure tests and Clippy passed through `9c1a4a0`. The in-memory desktop demo started on X11. The exact `3151:4015` `hidraw2` descriptor is allowlisted by the helper, and the app accepts complete 64-byte payloads or complete 65-byte replies with a leading zero. On 2026-09-23, the node had the active-user ACL; a raw read-only `0x80` exchange returned 65 bytes including the leading zero. The normal-user CLI identified firmware `0x0100`, profile 0; keymap reads were byte-stable, all feature-section reads passed, and two historical archive captures compared `[]` (then-current capture used two sweeps). | Current-source Linux build/runtime, picture selector context, GUI, Wayland, and physical write/readback/restoration remain pending (see `linux-handoff.md`) |
| Plug-and-play Nia87 TKL | Attached device auto-loads built-in 87-key geometry and fixed slots. The user observed the Iced release opening directly with verified readback and confirmed the TKL-shaped board and key selection. A physical unplug/replug was observed by read-only HID inventory, and a fresh keymap read matched the before capture byte for byte. Each connection's executor binds an immutable candidate identity; every transaction open, including recovery readback, checks that identity. Multiple matches and enumeration errors are explicit; headless reconnect tests retain staged drafts and reject stale results. Failed feature writes and conflicts require a manual read after reconnect; a multi-panel conflict test now preserves both specific warnings through disconnect. | Clean-machine and Linux GUI acceptance; stronger board/revision identification, reused-path identity limits and rendered cross-panel reconnect validation |
| Base and Fn keymaps | Both maps read twice; base, Fn-only and mixed same-slot application writes/restores pass with one-second setter spacing. The independent CLI also applied a single base A→B edit to slot9 with backup/readback, then restored A from a fresh revision; complete before/after archives compared `[]`. | Broader action coverage, physical output and persistence |
| Modifier, media and mouse actions | Pure codecs and catalog, including 19 selected official keyboard-catalog actions with documented wire facts. Three further system power/sleep/wake records now have named catalog entries and exact-byte descriptor tests; they have no physical output acceptance. Iced composes one- and two-modifier shortcuts from backend-advertised choices; a separate memory keyboard stages Ctrl+C and applies Ctrl+Shift+C with verified readback, and the Nia87 codec round-trips both. | Physical output and broader action behavior coverage, especially system controls that could affect host power |
| Macro storage | Exact 248-byte → short → empty replacement verified in slots1/24/49; 33 mixed key/mouse/movement events and delay boundaries stored exactly in slot49, then full configuration restoration verified; official short-macro capture now has a complete 64-byte replay regression. The official configurator advertises 50 slots (0–49). A read-only slot50 probe returned a stable but invalid macro buffer; no setter or binding was sent and a complete archive remained unchanged. The independent CLI twice applied a two-event macro to unbound slot49 with durable backup and full readback, then restored its original empty/count-zero raw bytes. The new product CLI native-backup restore path completed the second cycle; complete before/after archives compared `[]`. On the Linux-attached unit at `cc03b34`, read-only `list-macros` reported capacity 50, no configured slots, and next free slot-00. | Physical playback/timing and power-cycle persistence; repeat-zero and movement-delay semantics; do not infer usable slot50 storage from its getter alone |
| Macro editor | Native event table, focused keyboard/mouse-button recorder, repeats/modes, local labels, import/export and saved-slot binding; base/Fn binding modes read back and restore; recorder intervals attach to the preceding action; fixed-delay recording, reversible Clear draft and terminal-delay policy covered by pure/headless tests. Opening Macros now reads every advertised slot through one backend-neutral catalog operation. The library shows only stored programs and slots referenced by a keymap; Add chooses the first unbound empty slot and disables at the advertised capacity (50 on Nia87). The responsive grid reserves layout space for its scrollbar. Memory-backend tests cover catalog failure, stale results, Add and the full-capacity state. Toggle/hold require explicitly staged count 1. Counted mode stages 1–65,535 while existing raw zero remains readable and lossless; save/bind with zero is blocked. | Rendered GUI interaction validation; physical playback modes and timing, including repeat-zero semantics |
| Global lighting | Native controls and selected ID0–19 cases pass write/read/restore. Webcam evidence confirms steady red/green. Physical Iced steady-effect selections (Always On, red preset, hue drag) visibly changed the board. The independent CLI applied and restored steady green with durable backup and readback. Ordinary lighting writes report transport acceptance after known pacing; they do not imply an immediate getter. | Broader visual/option coverage and reactive effects; Iced write/read/restore and host-stream physical acceptance; identify earlier lighting drift cause |
| Per-key colors | Research captures establish selector-dependent picture responses. The physical selector audit and archive restoration are retained in `Research/picture-selector-audit.md`. The rebuilt Iced path physically submitted repeated full seven-page pictures and visibly changed colors; a later explicit read matched all 128 entries. The 2026-09-25 camera capture from the reviewed build shows retained-brush painting on F2 and F3. These are bounded observations, not rapid-change, persistence, or recovery acceptance. Ordinary submissions report transport acceptance, keep a cached before-image, and allow an explicit later read; selector changes invalidate cached picture data. | Linux current-source context/runtime; CLI picture write; resolve the earlier failed recovery discrepancy; power-cycle persistence. Full archive restore remains deferred from the public UI, with its raw-white mismatch unresolved |
| Host-driven lighting | Captured actual0D music/0E screen commands; retained Windows screen/WASAPI streams passed hardware/camera/restoration checks. Iced screen-average and playback music use the same pure session lifecycle, bounded executor frames and selected-target Nia87 adapter; Stop and close require restoration, with headless failure/ordering tests. Music has capability-driven temporary brightness, option and fixed RGB/rainbow controls with pure report tests. A recognized stored host mode can now be explicitly replaced with an onboard effect through the guarded lighting contract; unknown modes remain opaque. Linux samplers cross-build. | Physical Iced screen/music streaming, parameter and stored-mode exit acceptance; close/focus-loss/disconnect acceptance, sustained streaming, Wayland capture, Linux runtime/audio routing, X11 disconnect handling |
| Other settings | Native settings pass reversible complete readback. CLI debounce apply/restore passed with durable backup and complete readback. Iced exposes seven fields with one-field staged saves; connection refresh reads scalar sections once, and navigation sends no reads. The 2026-09-25 official-app capture observed six setting setters without immediate GET; this is reference behavior only. | Iced physical settings write/read/restore and rendered acceptance; physical sleep behavior and remaining option semantics; report-rate metadata remains unexposed (see `Research/report-rate-selected-path.md`) |
| Local configurations | Public Iced page captures and exports a diagnostic native archive using one complete sweep. Full archive import/review/restore is deferred from the public pre-alpha UI by user direction; automatic per-feature before-image backups remain. Core/device restore APIs, tests and research examples remain available to developers outside the public workflow. The independent CLI archive matches prior bytes, and offline comparison found only the known lighting change; those dated captures predate the current single-sweep behavior. Picture data represents only its captured selector. | Raw-white restore mismatch and older Windows collateral changes remain unresolved; establish multi-selector archive semantics before any future public restore workflow |
| Robust device transactions | Expected-state checks, disk backups, readback/rollback and OS-held interprocess lock; optional blocking Windows USB string reads removed | USB error recovery, disconnect/reconnect and partial failure tests |
| 2.4 GHz | Deferred by USB-first scope | Receiver identification, transport capability and separate verification when available |

Iced screen-average and playback music share a distinct host stream lifecycle.
Physical Iced acceptance remains open. Host
lighting is separate from per-key RGB picture storage.

Firmware flashing, vendor account login and cloud sharing are outside the local stock-firmware configurator scope. OEM database metadata and UI arrangements are not product implementation sources. Further physical output and write-recovery tests remain explicitly pending while the user is AFK.

### 2026-09-23 workspace responsiveness update

The user prioritized UX during the Windows session. The keyboard now stays in
the workspace across configuration pages; key assignments have an explicit action
list, lighting parameters have their own visible pane and RGB presets, per-key
RGB drafts preview on the board, and settings show all fields together. Tab
navigation sends no reads. Connection loads scalar sections once and starts a
passive macro scan; queued foreground operations run between individual slot
reads. The full scan still has the existing USB read pacing. Display selection
and optional point sampling are also implemented for host screen color.

The release was opened for user validation. Native tests and Clippy passed;
core WebAssembly and Linux devices cross-checks passed. Rendered/physical
acceptance of the new workspace and Linux runtime remain pending. The earlier
picture/recovery gate remains open. See the updated Iced lighting and macro
acceptance notes for exact scope.
